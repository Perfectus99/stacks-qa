{
  "name": "@stacks/regression",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --grep-invert @pending",
    "test:smoke": "playwright test --grep @smoke",
    "report": "playwright show-report"
  }
}
